/**
 * ThingSpace Connectivity Management APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, string, validateAndMap } from '@apimatic/schema';
import { type CallToolResult } from '@modelcontextprotocol/sdk/types.js';
import {
  carrierActionsRequestSchema,
  deviceManagementResultSchema,
  type DeviceManagementResult,
  type CarrierActionsRequest,
} from 'thingspace-connectivity-management-apilib';
import { schemas } from '../../schema.js';
import { deviceManagementController } from '../../sdk-client.js';
import { createErrorMessage, stringifyRawJson } from '../../utils.js';
import { type ToolDefinition } from '../index.js';


interface SuspendServiceForDevicesMCPToolRequest {
  'body': CarrierActionsRequest,
}

const suspendServiceForDevicesMCPToolRequestSchema = object({
  body: ['body', lazy(() => carrierActionsRequestSchema)],
});

async function handleSuspendServiceForDevices(
  args: unknown | SuspendServiceForDevicesMCPToolRequest
): Promise<CallToolResult> {
  const mappingResult = validateAndMap(args as any, suspendServiceForDevicesMCPToolRequestSchema);
  if (mappingResult.errors) {
    return {
      content: mappingResult.errors.map(error => ({
        type: "text",
        text: stringifyRawJson(error)
      })),
      isError: true,
    };
  }

  try {
    const response = await deviceManagementController.suspendServiceForDevices(
      mappingResult.result['body']
    );
    return {
      content: [
        {
          type: "text",
          text: stringifyRawJson({
            statusCode: response.statusCode,
            responseHeaders: response.headers,
            result: response.body
          }),
        },
      ],
    };
  } catch (error) {
    return createErrorMessage(error);
  }
}

export async function deviceManagementSuspendServiceForDevicesTool(): Promise<ToolDefinition> {
  return {
    tool: {
      name: "deviceManagement-suspendServiceForDevices",
      description: `Suspends service for one or more devices.`,
      inputSchema: await schemas.loadSchema("Device Management", "SuspendServiceForDevices"),
    },
    handler: handleSuspendServiceForDevices,
  };
}

